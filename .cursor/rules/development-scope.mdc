---
description: FreeFight 开发范围边界，禁止未授权的功能扩展
alwaysApply: true
---

# FreeFight 开发范围（禁止自由扩展）

本规则约束本仓库内一切开发与 AI 辅助改动。**未在下方「允许范围」中列出的内容，默认不得实现**；用户明确要求或更新本规则文件后方可扩展。

## 必须遵守

1. **先问再做**：需求含糊、超出允许范围、或会改变核心玩法时，先向用户确认，不要自行加功能。
2. **最小改动**：只改与当前任务直接相关的文件；不顺手重构、不顺手「顺便优化」无关模块。
3. **不扩计划外内容**：不要添加用户未要求的系统（例：技能树、关卡编辑器、联网、存档、剧情、音效、正式美术资源）。
4. **保持线框 Demo 定位**：UI 继续用 `ColorRect` / `Panel` / `Label` 等线框表现；不引入外部美术包替代整体风格，除非用户明确要求。
5. **架构约定不可擅自推翻**：
   - 战斗单位：`CombatUnit` 薄基类；`Hero` 有 `EquipmentInventory`；`Monster` 无装备。
   - 配置用 `Resource`（`MonsterData` / `EquipmentData` / `CombatStats`）；跨场景状态用 `GameManager` / `DataRegistry` Autoload。
6. **没有怪物自动生成**：场上怪物**只能**由玩家拖拽手牌部署产生；不得实现定时刷怪、波次刷怪、环境刷怪或任何不消耗手牌的自动召唤逻辑（含 `MonsterSpawner` 及同类系统）。

## 当前允许的功能范围（初版 Demo）

| 模块 | 允许内容 |
|------|----------|
| 主菜单 | 开始游戏、图鉴、场景切换 |
| 图鉴 | 基于 `GameManager` 解锁列表；未解锁显示 `???` |
| 战斗 | 左侧固定英雄；**拖拽手牌至右侧战场松手**部署；**留牌持仓负面**（战斗耦合叠加）；**弃牌**（10s 冷却、无代价）；无自动刷怪 |
| 英雄 | 攻/防/攻速；v2 基准 **120 HP**；持仓负面降低有效属性；失血（哥布林等）；装备格 |
| 怪物 | 仅玩家部署；`hold_penalty` / `hold_bleed_per_sec` 按物种配置（见 08 数值） |
| 掉落 | 装备（进英雄背包）或怪物卡（进手牌，上限 7）；**点击拾取** |
| 失败 | 英雄 HP ≤ 0 失败；可重新开始或回主菜单 |

## 明确禁止（除非用户更新本规则）

- 点击手牌即时部署、部署格 UI、固定槽位点击部署
- **怪物自动生成**（任何形式）：`MonsterSpawner`、定时器刷怪、波次刷怪、环境怪、不消耗手牌的召唤
- 技能系统、Buff、元素克制、复杂 AI
- 波次胜利条件、关卡进度、多人、存档
- 将装备逻辑放进 `CombatUnit` 基类或给 `Monster` 加装备槽
- 大规模重命名目录、替换引擎版本、引入新框架

## 已移除机制（勿恢复）

用户已明确要求去掉，**不得在无指示情况下加回**：

- `DeploySlot` / 部署格交互
- `MonsterSpawner` / 自动刷怪 / 怪物自动生成（用户已多次明确要求移除）

## 数据与内容扩展

- **可加**：在 `resources/monsters/`、`resources/equipment/` 增加 `.tres` 配置（数值、名称、颜色），不改编排流程。
- **慎加**：新脚本、新 Autoload、新场景——须与上表「允许范围」一致，且改动面尽量小。

## 与用户协作

- 用户说「后续会添加更多 rules」：新约束应写入 `.cursor/rules/` 新 `.mdc` 文件，或经用户同意后再改本文件。
- 若实现与口头需求冲突，**以用户最新说明 + 本规则为准**。
